#include <vector>
#include <string>

namespace sinn {

  namespace util {

    double sumsqdiff(std::vector<double> a, std::vector<double> b);
    std::string vec2string(std::vector<double> vec);

  } // namespace util

} // namespace util
